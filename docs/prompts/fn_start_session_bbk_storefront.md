# PROMPT FUNCTION: fn_start_session_bbk_storefront

> **Target Entity:** `bbk-storefront` (Public E-Commerce & Customer Conversion Engine)  
> **Physical Path:** `c:\Users\Lenovo\Documents\Github\JARVIS\bbk-storefront\`  
> **Authority:** `README.md` & `docs/progress/`  
> **Execution Mode:** Zero-Search 1-Step Bootstrap

---

## 1. Executive Purpose
Initializes a sovereign frontend development, SEO tuning, or conversion engineering session in `bbk-storefront`. Guarantees immediate session file creation (`docs/progress/CHAT-X.Y.md`), reads current checkpoint state, and eliminates repetitive onboarding.

---

## 2. 4-Stage Atomic Execution Protocol

### ? STEP 1: STATE INGESTION
1. Determine current timestamp in WIB (`YYYY-MM-DD HH:MM WIB`).
2. Read latest checkpoint in `README.md` and `docs/progress/README.md`.
3. Identify next session identifier (e.g. `CHAT-2.9.md`).

---

### ?? STEP 2: AUTOMATIC SESSION FILE CREATION (MANDATORY)
1. **In Antigravity (PC):** Immediately create the file at `docs/progress/<NEXT_CHAT>.md` with the session header.
2. **In ChatGPT (Web/Mobile):** Emit the full Markdown document block for `<NEXT_CHAT>.md` directly in the first turn.

---

### ?? STEP 3: EMIT BOOTSTRAP BANNER

```text
========================================================================================
??? BBK STOREFRONT SESSION // <NEXT_CHAT>
?? WAKTU MULAI      : <YYYY-MM-DD HH:MM WIB>
?? TARGET ENTITAS   : bbk-storefront (Public Frontend Next.js 15)
?? STATUS TERAKHIR  : ?? Ready for Execution | Sales Helper + Hierarchy + SEO Parity
========================================================================================
```

---

### ?? STEP 4: PRESENT ACTIONABLE STOREFRONT AGENDA
The AI immediately presents the frontend development agenda:

```text
Agenda fokus sesi ini:
[1] ?? Sales Helper & Quote Integration (BTC Proxy & Pricing Bounds)
[2] ?? Product Catalog & Split-Fetch (READY vs SOLD Boundary)
[3] ??? SEO & Universal Hierarchy Resolver (WordPress/WooCommerce Parity)
[4] ?? UI/UX Hardening & Mobile Optimization
[5] ??? Custom Task (Sebutkan task yang mau lu kerjakan...)
```
